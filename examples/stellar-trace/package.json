{
  "name": "stellar-trace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Index Stellar contract events into SQLite — a resumable getEvents ingest loop that stores raw XDR.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "//": "`build` typechecks rather than emitting: nothing here is published, and the repo's generic matrix runs `npm run build --if-present`, so this is where a type error gets caught in CI.",
    "build": "tsc --noEmit",
    "ingest": "tsx src/bin/ingest.ts",
    "trace": "tsx src/bin/trace.ts",
    "test": "node --import tsx --test test/*.test.ts",
    "check:restart": "tsx scripts/check-restart.ts"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "16.2.0",
    "better-sqlite3": "12.11.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20",
    "tsx": "^4.23.12",
    "typescript": "^5"
  }
}
